server :
  port : 8085

spring:
  application:
    name: order-service
    
  kafka:
    bootstrap-servers: localhost:9092

eureka :
  client :
    service-url:
      defaultZone : http://localhost:8761/eureka
